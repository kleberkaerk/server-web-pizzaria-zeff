spring:

  datasource:
    url: jdbc:mysql://localhost:3306/PIZZARIA_ZEFF?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none

server:
  error:
    include-stacktrace: never
    whitelabel:
      enabled: false